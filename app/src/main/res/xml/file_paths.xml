<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Cache dir for shareable diagnostic log files (autovakt_diagnostics.txt) -->
    <cache-path name="autovakt_logs" path="." />
    <!-- Files dir for persisted crash reports (autovakt_last_crash.txt) -->
    <files-path name="autovakt_crashes" path="." />
</paths>
