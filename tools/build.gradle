plugins {
    id 'org.jetbrains.kotlin.jvm'
    id 'application'
}

application {
    mainClass = 'MockAbrpServerKt'
}

// Source lives flat in tools/ — no src/main/kotlin subfolder needed
sourceSets {
    main {
        kotlin {
            srcDirs = ['src']
        }
    }
}
